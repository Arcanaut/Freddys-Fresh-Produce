//This will appear when the user tries to navigate to a page that doesn't exist.
import React from 'react';

const NoMatch = () => {
  return (
    <div>
      Oops, we couldn't find that page.
    </div>
  );
};

export default NoMatch;